{
  "name": "credit-sea-lms",
  "version": "1.0.0",
  "private": true,
  "description": "Loan Management System — Next.js borrower portal + Express operations API",
  "scripts": {
    "install:all": "npm install && npm install --prefix server && npm install --prefix client",
    "dev": "concurrently -n server,client -c blue,magenta \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "build": "npm run build --prefix server && npm run build --prefix client",
    "seed": "npm run seed --prefix server",
    "typecheck": "npm run typecheck --prefix server && npx --prefix client tsc --noEmit -p client/tsconfig.json",
    "lint": "npm run lint --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "license": "MIT"
}
